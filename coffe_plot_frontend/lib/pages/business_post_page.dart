import 'package:flutter/material.dart';

class BusinessPostPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Business Post Page'),
      ),
      body: Center(
        child: Text('Business Post Page'),
      ),
    );
  }
}